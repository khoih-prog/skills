{
  "name": "proactive-amcp",
  "displayName": "Proactive AMCP",
  "version": "0.9.3",
  "description": "Make your agent proactive — auto-checkpoint before death, resurrect from IPFS, self-heal via watchdog.",
  "requiredEnvVars": [
    {
      "name": "SOLVR_API_KEY",
      "description": "Solvr API key for agent registration and free IPFS pinning (get via 'solvr register')",
      "required": true
    },
    {
      "name": "PINATA_JWT",
      "description": "Pinata JWT for direct IPFS pinning (alternative to Solvr)",
      "required": false
    },
    {
      "name": "NOTIFY_TARGET",
      "description": "Telegram chat ID for resurrection notifications",
      "required": false
    },
    {
      "name": "GROQ_API_KEY",
      "description": "Groq API key for smart checkpoint filtering and memory pruning",
      "required": false
    }
  ],
  "requiredBinaries": ["bash", "curl", "jq"],
  "optionalBinaries": ["git", "systemctl", "crontab"],
  "persistence": {
    "createsSystemdUnits": true,
    "createsCronJobs": true,
    "createsIdentityFiles": true
  },
  "networkAccess": {
    "endpoints": [
      {
        "host": "api.solvr.dev",
        "purpose": "Agent registration, knowledge search, IPFS pinning"
      },
      {
        "host": "api.pinata.cloud",
        "purpose": "Direct IPFS pinning (alternative to Solvr)"
      },
      {
        "host": "ipfs.solvr.dev",
        "purpose": "IPFS gateway for checkpoint retrieval"
      }
    ]
  },
  "dataExposed": "Checkpoints contain workspace files, memory, config. Plaintext by default."
}
